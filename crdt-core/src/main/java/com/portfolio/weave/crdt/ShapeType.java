package com.portfolio.weave.crdt;

/** The kinds of objects a weave canvas can hold. Set once at creation and never changes. */
public enum ShapeType {
	RECT, ELLIPSE, STICKY, TEXT, ARROW, PATH, IMAGE
}
